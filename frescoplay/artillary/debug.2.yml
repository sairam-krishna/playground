config:

  target: "https://reqres.in"

  environments:

    debug:

      phases:

        - duration: 60

          arrivalCount: 10

scenarios:

  - name: "Debug Scenario"

    flow:

    - post:
        url: "/api/users"
        json:
          name: "sai"
          job: "Software Engineer"
